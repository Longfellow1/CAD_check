# 增量缺陷表单

用途：记录真实 CAD MVP 的可复现缺陷。每条记录必须能从测试输入追溯到证据、根因和关闭条件；“接口返回 200”不等同于功能验收通过。

## 1. 测试批次基线

| 维度 | 内容 |
|---|---|
| 测试批次 | `INC-20260910-SCANIA-UI-UPLOAD` |
| 执行日期 | 2026-09-10 |
| 分支 / 提交 | `fix/viewer-manual-test` / `c815b7b711a3137deea2960c1c4998ae004bc381`；本轮 WebUI 改动尚未提交 |
| 主机与运行时 | macOS；Python 3.13.3；`cadquery-ocp 7.9.3.1.1`；`ocp-tessellate 3.5.0` |
| 服务 | `http://127.0.0.1:8000`；Uvicorn `no-reload`；开发手测实例 |
| 主要输入 | `fixtures/public-datasets/scania-8x4/source/Scania-8x4.stp`；294,668,730 bytes；SHA-256 `573aa7da05462dee25a430b28a6869906775ae5e0126308956661c1e1da421b7` |
| Scania 读取基线 | `CONFIG_CONTROL_DESIGN`；mm；644 occurrences；564 leaves；22 个无效 B-Rep 记录 |
| 自动化基线 | 本轮后：`pytest -q`：27 passed、1 skipped；`npm run build`：通过 |
| 修复前浏览器基线 | Chrome + Playwright；自动加载 Scania 时会触发完整 Viewer payload；进入“快速测量”后模型下拉才包含 `SCANIA` |
| 判定原则 | 结果正确性、用户可发现性、证据完整性和资源安全均属于验收条件 |

## 2. 状态与分级口径

| 项目 | 口径 |
|---|---|
| 严重度 S1 | 可能导致服务不可用、结果可信度受损或资源耗尽 |
| 严重度 S2 | 核心测试流程阻断，但不影响其他流程 |
| 严重度 S3 | 局部能力降级，有替代路径 |
| 优先级 P0 | 本轮 MVP 手测或真实数据闭环前必须处理 |
| 优先级 P1 | 本轮可带限制验收，但下一轮必须处理 |
| 状态 | `已确认`：稳定复现；`待修复`：已定位但未合入；`待复测`：修复后等待验证；`关闭`：自动化和人工证据均通过 |

## 3. 缺陷主表

| 缺陷 ID | 测试用例 / 批次 | 首次 / 复测 | 测试层级 | 模块 / 接口 | 前置条件与输入 | 操作步骤 | 预期结果 | 实际结果 | 证据 ID / 位置 | 根因 / 定位 | 影响 | 复现性 | 严重度 | 优先级 | 建议归属 | 状态 / 关闭条件 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CAD-INC-001 | `TC-SCANIA-READINESS-001` / `INC-20260910-SCANIA` | 09-09 / 09-10 | 数据接入、Readiness | `POST /api/models/register`；`/api/models/SCANIA/readiness` | Scania STEP；SHA 与基线一致 | 注册模型并读取 readiness | 能读取模型；无效几何有明确降级策略；可区分可视化与工程校验资格 | 注册 HTTP 成功；readiness=`BLOCKED`；644 occurrences、564 leaves；22 个无效 B-Rep；无 SCANIA8X4 Binding | `E-SCANIA-READINESS-01`；API 响应；`fixtures/public-datasets/scania-8x4/pressure-test-report.json` | 输入数据包含无效 B-Rep；当前无该版本业务 Binding；系统以整体 BLOCKED 返回 | 只能评估读取/可视化，不能直接做正式规则校验 | 1/1 | S2 | P0 | 后端 + 测试数据准备 | 已确认；补充无效件降级和 Binding 后复测 |
| CAD-INC-002 | `TC-SCANIA-VIEWER-001` / `INC-20260910-SCANIA` | 09-09 / 09-10 | 性能、资源 | 全量 `/api/models/SCANIA/viewer` | Scania 全部 564 leaves；24 GB 主机 | 触发全量 tessellation 与 Viewer JSON 序列化 | 有资源预检；按子装配或 LOD 加载；超预算时可解释地阻断 | 网格化约 152 秒；峰值 RSS 约 5.60 GB；Viewer JSON 约 1.56 GB；约 3,984 万 triangles | `E-SCANIA-PERF-01`；`pressure-test-report.json`；`pressure-test-summary.md` | 当前一次性处理全部叶子件并返回单体 payload | 浏览器长时间无响应；并发时有进程/内存耗尽风险 | 已复现 1 次 | S1 | P0 | 后端 Viewer / 部署 | 已确认；完成预检、分块/LOD、超时取消后复测 |
| CAD-INC-003 | `TC-SCANIA-TREE-001` / `INC-20260910-SCANIA` | 09-09 / 09-10 | 结构保真、Viewer | XCAF 装配树 → Viewer payload | XCAF 读取到 5 层树 | 对比 XCAF occurrence 树与 Viewer 节点 | Viewer 保留装配层级、实例关系和可定位路径 | 后端有 644 occurrences、5 层树；Viewer payload 被压成 564 个叶子 root parts | `E-SCANIA-TREE-01`；XCAF 统计与 payload 统计 | `viewer_payload()` 只使用 `leaf_occurrences` | 复杂车型无法按总成定位、隐藏和复核 | 1/1 | S2 | P0 | 后端数据契约 + 前端 Viewer | 已确认；树结构和节点 ID 对齐后复测 |
| CAD-INC-004 | `TC-EVIDENCE-ALL-001` / `CONTROLLED-V1-V2` | 09-09 / 09-10 | Evidence、回归 | Evidence Viewer | Controlled 18 条案例已生成 Run | 逐条请求 Evidence Viewer | 每条结果都有 A/B、测量线、数值和可保存截图 | 16/18 返回 200；`CLR_MCU_BRACKET`、`CLR_MCU_MOTOR` 返回 500；两端重合导致零长度 Edge 构造失败 | `E-EVIDENCE-REG-01`；全量 Evidence API 复测；`pressure-test-summary.md` | 未保护零长度测量线，`BRepBuilderAPI_MakeEdge` 构造失败 | 两条回归案例无法形成完整证据闭环 | 2/2 | S2 | P0 | 后端 Evidence Renderer | 已确认；零长度转为明确状态或安全降级后复测 |
| CAD-INC-005 | `TC-FSAI-EXPLORE-001` / `FS-AI-ADS-DV` | 09-09 / 09-10 | 前端交互、测量 | Explore 对象选择、`/api/runs/explore` | FS-AI 真实装配；每个选择框约 2,573 项 | 不改默认值直接运行 Explore 并生成 Evidence | 默认对象应有业务 Binding；A/B 相同应被禁止或提示 | A/B 回落到同一首项；结果为 `0.00 mm`；Evidence 返回 500；明确选不同对象后可成功 | `E-FSAI-UI-01`；Chrome 复测；`fs-ai/pressure-test-report.json` | 无业务语义默认 Binding；缺少同对象校验和对象搜索 | 用户可能把无意义结果当成有效测量 | 2/2 | S2 | P0 | 前端交互 + 后端校验 | 已确认；加入搜索、默认 Binding、同对象阻断后复测 |
| CAD-INC-006 | `TC-FIAT-UNIT-001` / `FIAT-500` | 09-09 / 09-10 | 单位、结果可信度 | Explore 测量单位 | STEP 声明单位为 `m` | 读取模型并执行 Explore 测量 | 结果单位继承源模型；异常尺度需阻断或提示 | 源单位为 `m`，Explore 固定输出 `mm`；实测值 `1,123,415.157 mm` | `E-FIAT-UNIT-01`；`vehicle4em/pressure-test-report.json` | Explore 未传播源单位，使用固定默认单位；模型本身还有尺度疑点 | 可能污染工程测量值和 Evidence | 1/1 | S1 | P0 | 后端测量内核 | 已确认；单位传播和尺度门禁通过后复测 |
| CAD-INC-007 | `TC-METHOD-EVIDENCE-001` / `CONTROLLED-V1-V2` | 09-09 / 09-10 | 测量方法、证据表达 | directional distance、angle Evidence | 受控方向距离和角度案例 | 执行并查看 Trace、Viewer 和截图 | Evidence 明确标注基准、方向、几何标记和方法 | 方向距离使用 BBox 轴向近似；角度只有文字，没有角度弧线/基准几何 | `E-METHOD-EVIDENCE-01`；Trace 与 Evidence 复测 | Executor 与 Renderer 尚未表达完整工程测量几何 | 结果可读但不足以作为正式工程证据 | 1/1 | S3 | P1 | 测量内核 + Evidence Renderer | 已确认；补充真实方法语义和几何标记后复测 |
| CAD-INC-008 | `TC-UI-SCANIA-001` / `INC-20260910-SCANIA` | 09-10 / 09-10 | 浏览器可发现性、启动 | 首页、模型选择、`/api/models` | no-reload 服务；API 已含 `SCANIA`；访问 `127.0.0.1:8000` | 硬刷新首页；观察模型工作区；点击“加载 Scania” | 已注册模型应在首页有明确入口；重型 Viewer 不应在刷新时自动触发 | 修复前首页 `state.model` 固定为 `V2`，实际请求 `/api/models/V2/viewer`；顶部只有“查看 V1/查看 V2”；进入“快速测量”后下拉才出现 `SCANIA` | `E-UI-SCANIA-01`；Playwright 硬刷新 DOM/网络断言；`web/src/main.js` `boot()` | 默认回归模式绑定 V2；没有通用模型工作区；重型模型加载缺少用户确认 | 用户会误判 Scania 未导入，或在刷新时误触发重型加载 | 2/2 | S2 | P0 | 前端工作区 | 入口和显式加载门槛已自动化验证；待真实 Scania Viewer 手测后关闭 |
| CAD-INC-009 | `TC-UI-SCANIA-HEAVY-AUTO-001` / `INC-20260910-SCANIA` | 09-10 / 09-10 | 启动完整性、资源安全 | `boot()` → `/api/models/SCANIA/viewer` | Scania 已登记；564 leaves；全量网格约 1.56 GB JSON | 硬刷新首页，同时请求 `/api/health` | 刷新只读取目录和状态；健康接口保持可访问；重型网格必须由用户显式触发 | 修复前自动加载 Scania；完整 tessellation 为同步重任务，期间健康请求超时，页面表现为“没有模型/卡死” | `E-UI-SCANIA-BLOCK-01`；原复现终端；`tests/test_webui_model_discovery.py` 无自动请求断言 | `boot()` 自动调用 `loadPreviewModel()`；Viewer payload 生成缺少启动门禁 | 可能阻塞本地服务；并发时扩大内存和超时风险 | 1/1 | S1 | P0 | 前端加载策略 + Viewer 资源治理 | 已修复；硬刷新不再请求全量 Viewer，健康接口实测可达；真实大模型加载归入 CAD-INC-002 手测 |
| CAD-INC-010 | `TC-API-STEP-UPLOAD-001` / `INC-20260910-UPLOAD` | 09-10 / 09-10 | 数据接入、用户流程 | `POST /api/models/upload`；模型工作区 | 本地 `vehicle_v1.step`；约 192 KB | 通过上传入口选择 `.stp`，上传并加载 | 用户可上传 `.stp/.step`；服务保存、导入 XCAF、返回模型 key/readiness；非法扩展名被拒绝 | 修复前没有上传接口和文件入口；本轮小型真实 STEP 导入 200，登记后可查询模型，非法 `.txt` 返回 400；新模型无语义 Binding 时 readiness=`BLOCKED`，但 STEP 几何读取检查通过 | `E-UPLOAD-API-01`；`tests/test_model_upload.py`：2 passed | 缺少 multipart 上传和运行时模型登记通道 | 用户无法把自己的真实数模带入 MVP；无法复用 Viewer 验证 | 2/2 | S2 | P0 | 后端接入 + 前端工作区 | API 自动化通过；待浏览器选择真实 `.stp` 并完成 Viewer 手测后关闭 |
| CAD-INC-011 | `TC-UI-MODEL-LOAD-001` / `INC-20260910-UPLOAD` | 09-10 / 09-10 | 前端交互、模型加载 | V1/V2 按钮、模型下拉、`/api/models/{key}/viewer` | V1/V2 小模型；Scania 大模型；上传模型 | 硬刷新；选择模型；观察加载按钮和 Viewer | 所有模型都通过明确的“加载”动作进入 Viewer；切换选择不应隐式拉取 payload；V1/V2 仍可用于回归 | 修复前 V1/V2 按钮文案为“查看”，模型选择与加载语义混杂；本轮改为“加载 V1/V2”，下拉选择先显示等待页，需点击“加载”；V1/V2 回归语义未改变 | `E-UI-MODEL-LOAD-01`；Playwright 模型发现契约 | 缺少统一模型工作区和显式加载状态 | 用户不清楚何时会触发大模型加载，容易重复点击或刷新 | 2/2 | S2 | P0 | 前端交互 | 自动化通过；待用户以 Scania 和上传文件完成一次端到端手测后关闭 |

## 4. 修复前复测矩阵

| 用例 ID | 目标 | 修复前结果 | 通过标准 | 证据 |
|---|---|---|---|---|
| `TC-API-SCANIA-001` | 注册并列出 Scania | 部分通过：注册成功，readiness BLOCKED | 可解释返回可视化资格、工程校验资格和阻断原因 | `/api/models`、`/api/models/SCANIA/readiness` |
| `TC-UI-SCANIA-001` | 首页硬刷新后发现 Scania | 不通过 | 首页或默认工作区可直接选择 Scania，并显示当前模型状态 | `E-UI-SCANIA-01` |
| `TC-UI-EXPLORE-SCANIA-001` | 快速测量中选择 Scania | 通过入口验证；未主动请求全量 Viewer | 选择后可展示 readiness、资源预警，并在预算内加载 | Playwright 模型下拉断言 |
| `TC-SCANIA-VIEWER-001` | Scania 全量 Viewer | 不通过资源门禁 | 不因单次全量 payload 长时间占满服务或浏览器 | `E-SCANIA-PERF-01` |
| `TC-REGRESSION-CONTROLLED-001` | 受控 V1/V2 回归 | 主链通过；2 条 Evidence 失败 | 18 条结果均可复核并保存证据 | `E-EVIDENCE-REG-01` |

## 5. 本轮修复复测

| 复测项 | 操作与输入 | 预期 | 实际结果 | 证据 | 状态 |
|---|---|---|---|---|---|
| Scania 首页发现 | 硬刷新 `http://127.0.0.1:8000`；读取 `/api/models` | 首页列出已注册模型；不自动请求重型 Viewer；显示明确的加载入口 | 通过；`SCANIA · scania-r620-8x4` 出现在“模型工作区”，Viewer 显示“SCANIA · 等待加载”，无 Scania Viewer 请求；健康接口 5 秒内可达 | `E-UI-SCANIA-02`；`tests/test_webui_model_discovery.py`；浏览器契约：1 passed | 自动化通过 |
| Scania 显式加载 | 在模型工作区选择 SCANIA，点击“加载 SCANIA” | 只有点击后才请求 `/api/models/SCANIA/viewer`；成功后标题和状态指向 SCANIA | 通过；浏览器测试用 V2 小 payload 替代真实大 payload，确认点击触发、状态切换和回归切换；真实 1.56 GB payload 留待人工 | `E-UI-SCANIA-03`；同一浏览器契约测试 | 待人工真实大模型复测 |
| STEP 上传接口 | TestClient 上传 `data/step/vehicle_v1.step`，文件名 `headspace-demo.stp`；再上传 `.txt` | STEP 以流式方式保存并导入；返回 key、文件大小、readiness；非法扩展名拒绝 | 通过：合法文件 HTTP 200、模型登记成功、STEP/几何检查通过；`.txt` HTTP 400；新模型缺少 Binding 时明确 readiness=`BLOCKED` | `E-UPLOAD-API-01`；`tests/test_model_upload.py`：2 passed | 自动化通过 |
| STEP 上传前端入口 | 打开首页检查 `#step-upload`、`#upload-step`、accept 属性 | 用户能选择 `.stp/.step`，看到上传/导入状态，并在导入后进入三维加载 | 通过：入口可见，accept=`.stp,.step`；浏览器契约覆盖控件存在；真实文件选择和导入待人工 | `E-UPLOAD-UI-01`；`tests/test_webui_model_discovery.py` | 待人工复测 |
| V1/V2 加载语义 | 硬刷新后观察 V1/V2；分别点击“加载 V1”“加载 V2” | 不默认进入 V1/V2；点击按钮才加载对应小模型；回归仍使用 V1/V2 | 通过：按钮文案已明确为“加载”；Scania 存在时刷新显示等待页；回归测试 27 passed、1 skipped | `E-UI-MODEL-LOAD-01`；`web/src/main.js`；全量 pytest 输出 | 待浏览器人工复测 |

## 6. 当前 Scania 访问说明

- API 已注册：`SCANIA → scania-r620-8x4 / public-dataset`。
- 当前服务已注册 Scania；硬刷新首页会在“模型工作区”列出 `SCANIA · scania-r620-8x4`，但只显示等待页，不自动请求重型 Viewer。选择 SCANIA 后点击“加载 SCANIA”才开始网格化。
- V1/V2 按钮已改为“加载 V1”“加载 V2”，用于显式加载小型受控模型；“版本回归”仍固定使用 V1/V2。
- 页面新增“上传 STEP”入口，支持 `.stp/.step`。上传后模型只登记在当前服务会话内；新模型没有业务 Binding 时可预览，但工程校验 readiness 会保持 `BLOCKED`。
- 全量 Scania Viewer 约 1.56 GB JSON；自动加载期间页面可能等待 2–3 分钟，手测前应观察资源占用，不要重复刷新。
- 显式加载 Scania 期间仍应观察资源占用，不要重复点击；若失败记录浏览器状态、服务日志和 RSS。
- 该模型 readiness 为 `BLOCKED`，本轮可用于复杂度、装配树和 Viewer 评估，不应直接宣称已具备正式工程校验资格。

## 7. 关闭要求

缺陷关闭必须同时具备：

1. 修复提交或明确的配置变更；
2. 对应自动化回归结果；
3. 相同输入和步骤的浏览器/API 复测记录；
4. Evidence、日志或截图路径；
5. 对用户影响已消失，且没有把 `BLOCKED`、`REVIEW_REQUIRED` 误标为正式通过。

相关材料：

- `fixtures/public-datasets/scania-8x4/pressure-test-report.json`
- `fixtures/public-datasets/pressure-test-summary.md`
- `docs/MVP_TEST_DATA_READINESS.md`

## 8. 本次 Scania 流式 Viewer 复测（2026-09-10）

### 8.1 复测基线

| 维度 | 内容 |
|---|---|
| 测试批次 | `INC-20260910-SCANIA-STREAM-TREE` |
| 分支 / 提交 | `dev` / `a7c46737ec4d81dc85e81aef417fb44e198bc660` |
| 服务 | `http://127.0.0.1:8000`；`server.app_v2:app`；Uvicorn `--reload` |
| 输入 | `fixtures/public-datasets/scania-8x4/source/Scania-8x4.stp`；294,668,730 bytes；SHA-256 `573aa7da05462dee25a430b28a6869906775ae5e0126308956661c1e1da421b7` |
| XCAF 源结构 | 1 个根节点；644 occurrences；80 个父节点；564 个叶件；最大深度 5；10 个叶件 B-Rep 无效 |
| Viewer 衍生物 | 164 chunks；564 parts；Preview 缓存 57,653,562 bytes |
| 复测方式 | 服务日志、缓存元数据、XCAF 读取统计、Playwright 分段加载 |

### 8.2 结果表

| 缺陷 ID | 测试用例 | 预期 | 实际结果 | 证据 | 根因 / 影响 | 复现性 | 严重度 / 优先级 | 状态 |
|---|---|---|---|---|---|---|---|---|
| `CAD-INC-012` | `TC-SCANIA-STREAM-PERF-001` | 首块出现后持续加载；单块耗时可控；其他接口保持可用 | 164 块最终生成；实际 Tessellation 累计 `228.1s`；`chunk-0152` 单块 `213.3s`。生成期间缓存状态请求曾超过 10s 无响应；完成后缓存查询约 19ms | `.cadcheck/cache/viewer/573aa7da05462dee25a4-1-preview/`；服务日志；各 chunk `meta.tessellation_ms` | 同步请求内执行 OCCT Tessellation；单叶件耗时高度不均，阻塞服务事件循环 | 1/1 | S1 / P0 | 已确认，待修复 |
| `CAD-INC-013` | `TC-SCANIA-STREAM-UI-001` | 缓存命中时也应可连续交互；进度稳定；不应长时间假死或消失 | 分段验证：4 chunks/14 parts=`6.57s`；10/38=`21.02s`；20/64=`46.41s`。轮询多次超时；全量浏览器加载在 180s 内未完成；无稳定 JS exception | Playwright 分段加载输出；`web/src/main_v2.js` `loadChunk()` | 每个叶件调用 `viewer.addPart()`，未传 `{skipBounds:true}`；每次追加都重算 bounds、clipping、treeview 并重绘 | 1/1；人工观察到“先预览、后卡顿/消失” | S1 / P0 | 已确认，待修复 |
| `CAD-INC-014` | `TC-SCANIA-TREE-FIDELITY-002` | Viewer 树保留 XCAF 父子层级、名称和可定位路径 | 源有 644 nodes/80 parents/564 leaves；Viewer payload 为 564 个 root parts，嵌套 group=`0`；名称保留但路径被压成 `__`，完整层级仅留在 `meta.paths` | XCAF 读取统计；`manifest.json`；chunk JSON；`server/cad/view_derivative.py` | 衍生层只取 `leaf_occurrences`；`to_ocpgroup(named)` 后未按 XCAF 路径重建父节点；前端跨 chunk 只追加叶件 | 1/1 | S2 / P0 | 已确认，待修复 |

### 8.3 关闭条件

1. Tessellation 改为可观测、可取消的后台任务或预生成流程；任一叶件异常耗时不能阻塞健康检查和其他请求。
2. 前端批量追加叶件时使用 `skipBounds`，每批只做一次 `updateBounds`；在 564 叶件输入下首屏和持续交互时间满足可接受阈值。
3. Viewer 使用独立的 assembly tree model：父节点、叶节点、原始 occurrence path、实例名称可展开、隐藏和定位；不能只依赖扁平 mesh 名称。
4. 用同一 Scania 输入复测：首块可见、加载期间可旋转、进度连续、无空白/消失；完成后 564 叶件数量与源 XCAF 叶件数一致。

### 8.4 本次本地启动与运行日志

#### 启动信息

| 项目 | 记录 |
|---|---|
| 启动命令 | `./scripts/bootstrap.sh`；`./start.sh` |
| 实际入口 | `.venv/bin/python -m uvicorn server.app_v2:app --host 127.0.0.1 --port 8000 --reload` |
| PTY 会话 | `78001` |
| Uvicorn 进程 | reloader PID `15960`；应用 PID `15968`；启动时间 `2026-09-10 12:07:57 +0800` |
| 构建结果 | bootstrap 完成；Vite build 通过；pytest `30 passed, 1 skipped` |
| Scania 登记 | `POST /api/models/register` 返回 HTTP 200；644 occurrences；564 leaves；readiness=`BLOCKED` |

#### Uvicorn 原始关键日志摘录

Uvicorn 默认输出没有时间戳；以下为本次 PTY 会话中实际看到的关键顺序，省略了重复的连续 chunk 日志：

```text
GET /api/health HTTP/1.1                 200 OK
GET /api/models HTTP/1.1                 200 OK
POST /api/models/register HTTP/1.1       200 OK
GET /api/models/SCANIA/viewer/manifest?profile=preview   200 OK
GET /api/models/SCANIA/viewer/chunks/chunk-0000?profile=preview   200 OK
GET /api/models/SCANIA/viewer/chunks/chunk-0001?profile=preview   200 OK
...
GET /api/models/SCANIA/viewer/chunks/chunk-0151?profile=preview   200 OK
GET /api/models/SCANIA/viewer/chunks/chunk-0152?profile=preview   200 OK
...
GET /api/models/SCANIA/viewer/chunks/chunk-0163?profile=preview   200 OK
```

期间同时出现 OCCT/网格警告，例如：

```text
face 9899 ignored
face 1786 ignored
face 10 ignored
face 6 ignored
```

冷生成期间查询缓存状态的客户端输出：

```text
curl: (28) Operation timed out after 10004 milliseconds with 0 bytes received
```

这次超时发生在 chunk 生成过程中，不是 HTTP 500；生成完成后同一接口恢复正常：

```json
{"model":"SCANIA","profile":"preview","cached_chunks":164,"total_chunks":164,"cached_bytes":57653562}
```

#### 由缓存文件和 chunk 元数据补出的时间线

| 时间 / 指标 | 结果 |
|---|---:|
| `chunk-0000` 写入 | `12:14:37`；`tessellation_ms=1834.7` |
| `chunk-0151` 写入 | `12:18:06`；`tessellation_ms=67.4` |
| `chunk-0152` 写入 | `12:21:40`；`tessellation_ms=213329.6`，对应约 213.3 秒 |
| `chunk-0163` 写入 | `12:21:47`；`tessellation_ms=31.8` |
| 164 chunks 的 Tessellation 累计 | `228129.4ms`，约 228.1 秒 |
| 首末缓存文件时间跨度 | 430 秒；期间存在多次客户端请求，不能作为单次端到端耗时 |

#### 完成后的接口复核

```text
GET /api/health                         HTTP 200，约 0.003s
GET /api/models                         HTTP 200，约 0.003s
GET /api/models/SCANIA/viewer/cache     HTTP 200，约 0.003s
```

本日志用于排查 `CAD-INC-012`～`014`；重点关注 `server/streaming_api.py`、`server/cad/view_derivative.py`、`server/cad/tessellation.py`、`server/cad/step_reader.py` 和 `web/src/main_v2.js`。原始缓存目录为：

```text
.cadcheck/cache/viewer/573aa7da05462dee25a4-1-preview/
```
