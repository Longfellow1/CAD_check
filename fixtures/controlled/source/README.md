# Controlled Fixture generated outputs

This directory is intentionally waiting for the GPT-owned generator described in `docs/GPT_IMPLEMENTATION_REQUEST.md` and `../controlled-fixture-spec.yaml`.

Expected outputs include V1/V2 STEP files, mutation variants, an object inventory/binding manifest, and golden results. Do not hand-edit the generated STEP files or change the 12 mm / 8 mm golden contract to make an implementation pass.
