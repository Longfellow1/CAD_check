{
  "name": "cad-check-desktop",
  "private": true,
  "version": "0.3.0",
  "description": "Electron shell for CAD Check engineering verification MVP",
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --devtools",
    "check": "node --check main.cjs && node --check runtime.cjs && node --check preload.cjs && node --test runtime.test.cjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "electron": "37.4.0"
  }
}
